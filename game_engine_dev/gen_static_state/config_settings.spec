# config key : cardinality : static registry tag
# cardinality: SINGLE | LIST
# registry tag maps to static key type + name_to_idx callback (see config_settings_parse.cpp)

PATH_MP_TURN : SINGLE : U16
START_UNITS : LIST : UNIT
START_EXP_UNITS : LIST : UNIT
