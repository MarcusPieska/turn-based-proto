//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef [MACRO_PREFIX]_STATIC_DATA_H
#define [MACRO_PREFIX]_STATIC_DATA_H

#include <string>
#include "[MEMBER_TAG]_static_key.h"
#include "item_reqs.h"
#include "game_primitives.h"

//================================================================================================================================
//=> - [DATA_STRUCT] -
//================================================================================================================================

typedef struct [DATA_STRUCT] {
[STRUCT_MEMBERS]
} [DATA_STRUCT];

//================================================================================================================================
//=> - [CLASS_NAME] class -
//================================================================================================================================

class [CLASS_NAME] {
public:
    static void set_items ([DATA_STRUCT]* items, u16 item_count);
    static const [DATA_STRUCT]& get_item ([DATA_KEY] key);
    static u16 get_item_count ();

private:
    [CLASS_NAME] () = delete;
    [CLASS_NAME] (const [CLASS_NAME]& other) = delete;
    [CLASS_NAME] ([CLASS_NAME]&& other) = delete;

    static [DATA_STRUCT]* m_item_array;
    static u16 m_item_count;
};

#endif // [MACRO_PREFIX]_STATIC_DATA_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
