//================================================================================================================================
//=> - WARNING -
//================================================================================================================================
//
//  - This file is AUTO-GENERATED by generate.py
//  - Do not edit this file manually.
//
//================================================================================================================================
//=> - Includes and globals -
//================================================================================================================================

#include "city_job_parser.h"

//================================================================================================================================
//=> - CityJobParser implementation -
//================================================================================================================================

CityJobParser::CityJobParser (const StringManager& items, const NameToIdxCbs& map) : DataParserBase(items, map) {
}

CityJobStaticDataStruct* CityJobParser::parse_data_dependencies () {
    CityJobStaticDataStruct* parsed_data = new CityJobStaticDataStruct[m_item_count]();
    for (u32 i = 0; i < m_item_count; ++i) {
        StringManager line_items;
        get_line_items(get_raw_lines().get_string_content(i), line_items);
        parsed_data[i].food = parse_i16(line_items, 1);
        parsed_data[i].production = parse_u16(line_items, 2);
        parsed_data[i].commerce = parse_u16(line_items, 3);
        parsed_data[i].culture = parse_u16(line_items, 4);
        parsed_data[i].science = parse_u16(line_items, 5);
        parsed_data[i].religion = parse_u16(line_items, 6);
        parsed_data[i].slots = parse_u16(line_items, 7);
        parsed_data[i].effects = parse_item_effects_optional(line_items, 8);
    }
    return parsed_data;
}

//================================================================================================================================
//=> - End -
//================================================================================================================================

