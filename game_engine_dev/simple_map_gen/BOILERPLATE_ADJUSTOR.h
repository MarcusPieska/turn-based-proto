//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef ADJUST_[MACRO_TAG]_H
#define ADJUST_[MACRO_TAG]_H

#include "game_primitives.h"

//================================================================================================================================
//=> - [MAIN_CLASS_TAG] -
//================================================================================================================================

class Adjust_[MAIN_CLASS_TAG] {
public:
    explicit Adjust_[MAIN_CLASS_TAG] (u32 seed);

    bool adjust (u8* terrain, u16 w, u16 h);
    bool is_valid () const;

private:
    Adjust_[MAIN_CLASS_TAG] (const Adjust_[MAIN_CLASS_TAG]& other) = delete;
    Adjust_[MAIN_CLASS_TAG] (Adjust_[MAIN_CLASS_TAG]&& other) = delete;

    u32 m_seed;
    bool m_valid_adjust;
};

#endif // ADJUST_[MACRO_TAG]_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
