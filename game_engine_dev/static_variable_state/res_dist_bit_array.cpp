//================================================================================================================================
//=> - WARNING -
//================================================================================================================================
//
//  - This file is AUTO-GENERATED by generate.py
//  - Do not edit this file manually.
//
//================================================================================================================================
//=> - Includes and globals -
//================================================================================================================================

#include "res_dist_bit_array.h"

//================================================================================================================================
//=> - ResDistBitArray implementation -
//================================================================================================================================

ResDistBitArray::ResDistBitArray (u16 count) :
    m_flags(count) {
}

ResDistBitArray::~ResDistBitArray () {
}

bool ResDistBitArray::is_flagged (ResDistStaticDataKey key) const {
    return m_flags.get_bit(key.value()) != 0;
}

void ResDistBitArray::set_flag (ResDistStaticDataKey key) {
    m_flags.set_bit(key.value());
}

void ResDistBitArray::clear_flag (ResDistStaticDataKey key) {
    m_flags.clear_bit(key.value());
}

bool ResDistBitArray::can_access (u16 idx, ResDistStaticDataKey& out_key) const {
    if (idx >= m_flags.get_count()) {
        return false;
    }
    if (m_flags.get_bit(idx) == 0) {
        return false;
    }
    out_key = ResDistStaticDataKey::from_raw(idx);
    return true;
}

//================================================================================================================================
//=> - End -
//================================================================================================================================
