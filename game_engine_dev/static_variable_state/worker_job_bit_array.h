//================================================================================================================================
//=> - WARNING -
//================================================================================================================================
//
//  - This file is AUTO-GENERATED by generate_static_data_and_test.py
//  - Do not edit this file manually.
//
//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef WORKER_JOB_BIT_ARRAY_H
#define WORKER_JOB_BIT_ARRAY_H

#include "bit_array.h"
#include "game_primitives.h"
#include "worker_job_static_key.h"

class GeneralAssessor;
class KeyHolder;

//================================================================================================================================
//=> - WorkerJobBitArray class -
//================================================================================================================================

class WorkerJobBitArray {
public:
    explicit WorkerJobBitArray (u16 count);
    ~WorkerJobBitArray ();

    bool is_flagged (u16 index) const;
    void set_flag (WorkerJobStaticDataKey key);
    void clear_flag (WorkerJobStaticDataKey key);

    bool can_access (u16 idx, WorkerJobStaticDataKey& out_key) const;

private:
    WorkerJobBitArray (const WorkerJobBitArray& other) = delete;
    WorkerJobBitArray (WorkerJobBitArray&& other) = delete;

    BitArrayCL m_flags;
};

#endif // WORKER_JOB_BIT_ARRAY_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
