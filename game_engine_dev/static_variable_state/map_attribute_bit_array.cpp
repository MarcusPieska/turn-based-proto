//================================================================================================================================
//=> - WARNING -
//================================================================================================================================
//
//  - This file is AUTO-GENERATED by generate.py
//  - Do not edit this file manually.
//
//================================================================================================================================
//=> - Includes and globals -
//================================================================================================================================

#include "map_attribute_bit_array.h"

//================================================================================================================================
//=> - MapAttributeBitArray implementation -
//================================================================================================================================

MapAttributeBitArray::MapAttributeBitArray (u16 count) :
    m_flags(count) {
}

MapAttributeBitArray::~MapAttributeBitArray () {
}

bool MapAttributeBitArray::is_flagged (MapAttributeStaticDataKey key) const {
    return m_flags.get_bit(key.value()) != 0;
}

void MapAttributeBitArray::set_flag (MapAttributeStaticDataKey key) {
    m_flags.set_bit(key.value());
}

void MapAttributeBitArray::clear_flag (MapAttributeStaticDataKey key) {
    m_flags.clear_bit(key.value());
}

bool MapAttributeBitArray::can_access (u16 idx, MapAttributeStaticDataKey& out_key) const {
    if (idx >= m_flags.get_count()) {
        return false;
    }
    if (m_flags.get_bit(idx) == 0) {
        return false;
    }
    out_key = MapAttributeStaticDataKey::from_raw(idx);
    return true;
}

//================================================================================================================================
//=> - End -
//================================================================================================================================
