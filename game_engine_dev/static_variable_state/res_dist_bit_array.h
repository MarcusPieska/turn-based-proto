//================================================================================================================================
//=> - WARNING -
//================================================================================================================================
//
//  - This file is AUTO-GENERATED by generate_static_data_and_test.py
//  - Do not edit this file manually.
//
//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef RES_DIST_BIT_ARRAY_H
#define RES_DIST_BIT_ARRAY_H

#include "bit_array.h"
#include "game_primitives.h"
#include "res_dist_static_key.h"

class GeneralAssessor;
class KeyHolder;

//================================================================================================================================
//=> - ResDistBitArray class -
//================================================================================================================================

class ResDistBitArray {
public:
    explicit ResDistBitArray (u16 count);
    ~ResDistBitArray ();

    bool is_flagged (u16 index) const;
    void set_flag (ResDistStaticDataKey key);
    void clear_flag (ResDistStaticDataKey key);

    bool can_access (u16 idx, ResDistStaticDataKey& out_key) const;

private:
    ResDistBitArray (const ResDistBitArray& other) = delete;
    ResDistBitArray (ResDistBitArray&& other) = delete;

    BitArrayCL m_flags;
};

#endif // RES_DIST_BIT_ARRAY_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
