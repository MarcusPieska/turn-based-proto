//================================================================================================================================
//=> - WARNING -
//================================================================================================================================
//
//  - This file is AUTO-GENERATED by generate_static_data_and_test.py
//  - Do not edit this file manually.
//
//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef RESOURCE_BIT_ARRAY_H
#define RESOURCE_BIT_ARRAY_H

#include "bit_array.h"
#include "game_primitives.h"
#include "resource_static_key.h"

class GeneralAssessor;
class KeyHolder;

//================================================================================================================================
//=> - ResourceBitArray class -
//================================================================================================================================

class ResourceBitArray {
public:
    explicit ResourceBitArray (u16 count);
    ~ResourceBitArray ();

    bool is_flagged (u16 index) const;
    void set_flag (ResourceStaticDataKey key);
    void clear_flag (ResourceStaticDataKey key);

    bool can_access (u16 idx, ResourceStaticDataKey& out_key) const;

private:
    ResourceBitArray (const ResourceBitArray& other) = delete;
    ResourceBitArray (ResourceBitArray&& other) = delete;

    BitArrayCL m_flags;
};

#endif // RESOURCE_BIT_ARRAY_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
