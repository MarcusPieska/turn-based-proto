//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef RESOURCE_PARSING_H
#define RESOURCE_PARSING_H

#include "resource_io.h"
#include "resource_types.h"

//================================================================================================================================
//=> - ResParser -
//================================================================================================================================

class ResParser {
public:
    ResParser ();
    ~ResParser ();

    ResParser (const ResParser& o) = delete;
    ResParser (ResParser&& o) = delete;

    bool parse_file (const ResIoFile& file);
    void release ();
    const ResCatalog& catalog () const;

private:
    void rst ();

    ResCatalog m_cat;
};

#endif // RESOURCE_PARSING_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
