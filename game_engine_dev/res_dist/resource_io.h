//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef RESOURCE_IO_H
#define RESOURCE_IO_H

#include "game_primitives.h"
#include "opt_str_mng.h"

//================================================================================================================================
//=> - ResIoPath -
//================================================================================================================================

class ResIoPath {
public:
    explicit ResIoPath (cstr root_off);

    cstr resources_path () const;

private:
    ResIoPath () = delete;
    ResIoPath (const ResIoPath& o) = delete;
    ResIoPath (ResIoPath&& o) = delete;

    char m_root[128];
    char m_res[192];
};

//================================================================================================================================
//=> - ResIoFile -
//================================================================================================================================

class ResIoFile {
public:
    ResIoFile ();
    ~ResIoFile ();

    ResIoFile (const ResIoFile& o) = delete;
    ResIoFile (ResIoFile&& o) = delete;

    bool load (cstr path);
    u32 line_n () const;
    cstr line (u32 idx) const;

private:
    void rst ();

    StringManager m_raw;
    StringManager m_lines;
    u32 m_line_n;
};

#endif // RESOURCE_IO_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
