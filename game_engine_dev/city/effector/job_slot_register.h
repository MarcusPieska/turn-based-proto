//================================================================================================================================
//=> - WARNING -
//================================================================================================================================
//
//  - AUTO-GENERATED by generate_job_slot_register.py
//  - Do not edit this file manually.
//
//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef JOB_SLOT_REGISTER_H
#define JOB_SLOT_REGISTER_H

#include "effect_enabler.h"
#include "game_primitives.h"
#include "item_effects.h"

struct EffectCtx;

//================================================================================================================================
//=> - JobSlotEntry -
//================================================================================================================================

struct JobSlotEntry {
    EffectEnabler m_enabler; // Source that must be active
    u16 m_job_id; // city_job catalog index
    i16 m_amount; // COUNT addend, or PERCENTAGE of base
    ItemEffectsScope m_scope; // LOCAL treated as CITY at query time
    ItemEffectAmountMode m_mode; // COUNT or PERCENTAGE
};

//================================================================================================================================
//=> - JobSlotRegister -
//================================================================================================================================
//
//  Offline table of jobSlots(...) effects, indexed by job_id for O(1) range
//  lookup. capacity = base_slots + COUNT bonuses + PERCENTAGE-of-base bonuses
//  from active enablers. No per-city slot counters required.
//
//================================================================================================================================

class JobSlotRegister {
public:
    static constexpr u16 JOB_N = 14;
    static constexpr u16 ENTRY_N = 12;

    static i16 bonus (u16 job_id, u16 base_slots, const EffectCtx& ctx);
    static u16 capacity (u16 job_id, u16 base_slots, const EffectCtx& ctx);

    static const JobSlotEntry* entries ();
    static u16 entry_count ();
    static u16 job_count ();

private:
    JobSlotRegister () = delete;
    JobSlotRegister (const JobSlotRegister& other) = delete;
    JobSlotRegister (JobSlotRegister&& other) = delete;

    static const JobSlotEntry s_entry[12];
    static const u16 s_off[15];
};

#endif // JOB_SLOT_REGISTER_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
