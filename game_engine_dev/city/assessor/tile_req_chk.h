//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef TILE_REQ_CHK_H
#define TILE_REQ_CHK_H

#include "game_primitives.h"
#include "item_reqs.h"

class GameArraySimple;
struct AssessorCtx;

//================================================================================================================================
//=> - TileReqChk -
//================================================================================================================================

class TileReqChk {
public:
    static bool chk_req (u8 kind, u16 idx, const AssessorCtx& ctx);
    static bool chk_reqs (const ItemReqsStruct& reqs, const AssessorCtx& ctx);

private:
    TileReqChk () = delete;
};

#endif // TILE_REQ_CHK_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
