//================================================================================================================================
//=> - Include guards -
//================================================================================================================================

#ifndef WB_SHEET_H
#define WB_SHEET_H

#include "game_primitives.h"

//================================================================================================================================
//=> - WbSheet -
//================================================================================================================================
//
//  RAII checkout of one Whiteboard tile sheet; releases on destroy.
//
//================================================================================================================================

class WbSheet {
public:
    explicit WbSheet (i32 tile_n);
    ~WbSheet ();
    u16* get ();
    u16 rd (u32 i) const;
    void wr (u32 i, u16 v);
    bool ok () const;

private:
    WbSheet (const WbSheet& other) = delete;
    WbSheet (WbSheet&& other) = delete;
    WbSheet& operator= (const WbSheet& other) = delete;
    WbSheet& operator= (WbSheet&& other) = delete;

    u16* m_p;
};

#endif // WB_SHEET_H

//================================================================================================================================
//=> - End of file -
//================================================================================================================================
